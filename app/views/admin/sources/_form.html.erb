<%= form_for(@admin_source) do |f| %>
  <% if @admin_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_source.errors.count, "error") %> prohibited this admin_source from being saved:</h2>

      <ul>
      <% @admin_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_name %><br />
    <%= f.text_field :source_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
